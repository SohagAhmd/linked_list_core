# linked_list

Beginner-friendly Python project for linked lists.

Structure:
- three linked list types: `singly`, `doubly`, `circular`
- each type has its own package with `node.py`, list class file, `insert.py`, `delete.py`, `traversal.py`, and `main.py` for demos.

Use this repo to implement and experiment with linked list operations.
